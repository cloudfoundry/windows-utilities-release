---
name: enable_ssh
description: "Enables SSH on Windows"
templates:
  disable-ssh.ps1: bin/disable-ssh.ps1
  pre-start.ps1: bin/pre-start.ps1
  drain.ps1: bin/drain.ps1

packages: []

properties:
  enable_ssh.enabled:
    description: "If false this job will do nothing"
    default: true
  enable_ssh.firewall_rule_name:
    description: "the SSH firewall rule name"
    default: "SSH"
